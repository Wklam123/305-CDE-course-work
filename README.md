### WeatherApp
NodeJs Weather App
##Start Application
```
git clone 
cd WeatherApp
node index.js
```
##open browser
```
http://localhost:port  where port is given in console
```

##
need to install few compments
